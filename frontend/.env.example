VITE_API_BASE='/api' # Use same-origin requests with the Vite dev proxy when developing locally
VITE_API_PROXY_TARGET='http://localhost:3001' # Local backend target for the Vite dev proxy
# VITE_API_BASE="https://$CODESPACE_NAME-3001.$GITHUB_CODESPACES_PORT_FORWARDING_DOMAIN/api" # for GitHub Codespaces
# VITE_API_BASE='http://localhost:3001/api' # Use this URL only when you intentionally want cross-origin local dev.
